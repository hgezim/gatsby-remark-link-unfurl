export interface MetadataInterface {
  title?: string
  description?: string
  url: string
  video?: { url: string }
  // audio: ,
  image?: { url: string }
  logo?: string //url
  site?: string // site name e.g. Youtube
}
